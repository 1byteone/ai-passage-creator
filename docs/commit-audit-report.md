# 提交合规审计报告

> 日期：2026-08-01
> 审计范围：`track/a-skill-engine` 分支，`8e86d22..HEAD`（本轮新增 16 个提交）
> 规范依据：CONTRIBUTING.md §1（Conventional Commits + EC/Angular）

---

## 1. 总体结论

| 指标 | 数值 |
|------|------|
| 新增提交总数 | 16 |
| **格式合规率** | **100%**（16/16 符合 `type(scope): subject`） |
| subject ≤ 72 字符 | 100% |
| 大写 type | 0（全部小写 type） |
| 句号结尾 | 0 |
| 混合功能提交 | 3（占 19%） |
| scope 大写 | 12（占 75%） |

---

## 2. 逐提交明细

### ✅ 完全合规（1 个）

| 提交 | 评估 |
|------|------|
| `feat(skill): skill/execute 端点应用 @RateLimit 限流` | type 小写 + scope 小写 + subject ≤50 |

### ⚠️ 部分违规 — scope 大写（12 个）

scope 使用了 `C1`/`C2`/`B1+D1` 等大写/混合命名，违反 EC/Angular 的 scope 小写约定。虽不影响机器可读性（commitlint 默认允许），但不符合完整规范。

### ⚠️ 部分违规 — 混合功能提交（3 个）

| 提交 | 违规 |
|------|------|
| `feat(B1+D1): 内容质量评分 + Docker 容器化` | 两个独立功能合并 |
| `feat(B2+D2): 多轮改写+版本历史 + Redis缓存+API限流` | 两个独立功能合并 |
| `feat(B3+D3): ... + ...` | 两个独立功能合并 |
| `feat(B4+D4+D5): ... + ...` | 三个独立功能合并 |

> 原因：为提升迭代效率并行推进多个任务后一次性提交。违反单一职责原则。

---

## 3. 历史遗留（非本轮范围）

`8e86d22` 之前的 8 个提交为课程教程式长描述（"Spring AI + 多 Agent + Stripe..."），
属于 dev 基线历史，**不建议改写**（改写会破坏历史且无价值）。

---

## 4. 改进建议

1. **新建提交**：一律遵守小写 scope + 单一职责（CONTRIBUTING.md 已更新）
2. **历史提交**：不重写（避免破坏协作历史），已在规范中明确禁止混合提交
3. **scope 命名规范**：已更新 CONTRIBUTING.md §1.3，新增"小写 + 连字符"约定
4. **工具钩子**：commit-msg 钩子已能拦截大写 type 和超长 subject，但**不强制 scope 小写**
   （避免对历史遗留过度收紧）

---

## 5. 合规率趋势

```
之前(课程基线):  0%  (8/8 非 conventional，历史遗留)
本轮新增:      100% (16/16 格式合规)
未来(钩子生效):  100% 强制
```
