# AI Passage Creator 前端页面优化执行日志

> 日期：2026-07-25
> 当前阶段：P0 第一批执行中
> 本日志记录已经落地的代码级变更、验证结果和后续风险。

---

## 1. 本次已完成

### 1.1 样式入口收口

已将全局样式入口统一移动到 `frontend/src/main.ts`：

1. `@/styles/variables.css`
2. `@/styles/common.css`

这样后续 token、全局状态、可访问性和 reduced-motion 不再堆在 `App.vue` 中。

### 1.2 design token v1 起步

已扩展 `frontend/src/styles/variables.css`，新增：

1. 语义化表面层 token
2. 语义化文本层 token
3. 语义化边界层 token
4. 语义化状态层 token
5. 间距 token
6. 字体角色 token
7. 控件高度与触控目标 token

当前策略仍保留绿色品牌方向，但开始从“绿色主题变量”升级到“绿色作为强调色的系统”。

### 1.3 核心组件状态底线

已在 `frontend/src/styles/common.css` 中收口：

1. 全局基础排版
2. 全局链接状态
3. 全局 `focus-visible`
4. Ant Design Button 状态
5. Ant Design Input 聚焦状态
6. Ant Design Steps / Progress / Spin 主题覆盖
7. reduced-motion 兜底

### 1.4 App 壳职责收窄

已从 `frontend/src/App.vue` 移除重复的全局 CSS，`App.vue` 现在只负责应用配置和布局壳。

### 1.5 创作页明显 emoji 状态清理

已在 `frontend/src/pages/article/ArticleCreatePage.vue` 中清理明显的 emoji 状态表达：

1. 阶段提示图标改为数字编号
2. 完成日志从带符号表达改为专业状态文案

### 1.6 页面审计基线

已新增 `docs/superpowers/plans/2026-07-25-frontend-page-audit-baseline.md`，覆盖：

1. 首页、创作页、Skill 执行页、统计页的代码审计基线
2. 当前全局问题
3. P0 问题矩阵
4. 后续 375 / 768 / 1024 / 1440 截图验收计划

### 1.7 P0 页面 IA 重排方案

已新增 `docs/superpowers/plans/2026-07-25-frontend-page-ia-restructure.md`，冻结：

1. 首页 IA
2. 创作页桌面与移动 IA
3. Skill 执行页 IA
4. 第一批实现切入点

### 1.8 创作页移动端辅助信息区

已在 `frontend/src/pages/article/ArticleCreatePage.vue` 增加移动端辅助信息区，在 `992px` 以下展示：

1. 当前流程和阶段进度
2. 当前步骤说明
3. 配额状态
4. 当前选题
5. 最近 3 条执行日志

这让移动端不再只是隐藏左右侧栏，而是保留关键流程上下文。

### 1.9 首页回访入口前置

已在 `frontend/src/pages/HomePage.vue` 将最近创作区域前置到 Hero 后、AI 工具箱前，并调整为“继续最近创作”：

1. 已登录用户更快回到最近任务
2. 加载中也保留区域结构
3. 保持主 CTA 和工具入口不冲突

### 1.10 创作页输入区结构优化

已在 `frontend/src/pages/article/ArticleCreatePage.vue` 重构输入状态的一屏表单结构：

1. 将选题输入独立为主任务区，明确“必填”和输入说明
2. 将文章风格与配图方式收拢为创作设置区，桌面双列、移动端单列
3. 将配额状态移动到 CTA 附近，帮助用户在提交前理解是否可创作
4. 保留原有 `v-model`、VIP 限制、按钮 loading、禁用条件和提交逻辑
5. 增加 992px / 520px 响应式规则，避免窄屏横向滚动和按钮挤压

### 1.11 前端开发设计规范选项表

已新增 `docs/superpowers/plans/2026-07-25-frontend-design-dev-spec-options.md`：

1. 将后续前端优化拆成可选择的规范议题
2. 每个议题提供推荐选项、备选项、优点和缺点
3. 当前默认采用：首页转化链路优先、组件级规范、边定规范边改
4. 固化首页首屏、CTA、文案、卡片、响应式、动效和性能的第一批执行口径

### 1.12 首页首屏转化链路优化

已在 `frontend/src/pages/HomePage.vue` 优化首页首屏：

1. 从居中介绍型 Hero 调整为“左任务入口 + 右流程证据”的工作台式首屏
2. 主 CTA 保持为“开始创作”，次入口调整为“查看工具箱”
3. 新增“从选题到成稿”的流程预览，展示标题方案、大纲确认、正文与配图三个真实流程节点
4. 流程预览会跟随用户输入的选题同步展示
5. 将首页强承诺文案调整为可验证的流程收益描述
6. 增加 992px / 768px 响应式规则，移动端改为单列布局

### 1.13 第二轮需求对齐选项

已在 `docs/superpowers/plans/2026-07-25-frontend-design-dev-spec-options.md` 追加 `H01-H12`：

1. 覆盖首页任务表达、输入联动、回访优先级、证据来源、移动端保留、性能治理等议题
2. 每个议题包含推荐项、备选项、优点和缺点
3. 默认采用任务路径式能力区、输入同步预览、最近创作前置、路由拆分和稳定依赖分组

### 1.14 首页核心能力区任务链重构

已在 `frontend/src/pages/HomePage.vue` 替换原六个同构功能卡片：

1. 改为“输入选题 / 确认标题和大纲 / 生成正文与配图 / 继续编辑或导出”的四步任务链
2. 右侧新增成稿产物预览，展示当前选题与标题候选、章节大纲、正文段落、封面配图、历史记录
3. 能力区 hover 与 focus-visible 状态已覆盖，支持键盘聚焦检查
4. 768px 以下保留完整任务路径，隐藏非关键图标以减少挤压

### 1.15 路由与依赖分包治理

已完成第一批低风险性能治理：

1. `frontend/src/router/index.ts` 将登录页、注册页和用户管理页改为路由级懒加载
2. `frontend/vite.config.ts` 新增保守 `manualChunks`
3. Vue 运行时、Ant Design Vue、ECharts 和常用工具依赖拆入独立稳定 chunk
4. 暂不改 Ant Design Vue 全局注册和 ECharts 模块化导入，避免扩大本批回归面

### 1.16 根滚动与移动端首屏细节修复

已补齐视觉回归时发现的移动端滚动问题：

1. `frontend/src/styles/common.css` 将根横向裁切从 `overflow-x: hidden` 调整为 `overflow-x: clip`
2. 为 `html`、`body` 和 `#app` 补充根高度约束，避免 `body` 变成不可见的独立滚动容器
3. 375px 下 `document.scrollingElement` 已恢复为完整页面滚动，后续工具箱、核心能力和工作流摘要均可访问
4. `frontend/src/pages/HomePage.vue` 缩短首页选题输入占位文案，并补充输入内部省略规则，减少移动端贴边裁切感

---

## 2. 验证结果

### 2.1 构建

已运行：

```bash
npm run build
```

结果：通过。最近一次验证在根滚动修复和首页移动端输入细节优化后完成。

本机最终验证命令使用了 `NODE_OPTIONS=--max-old-space-size=6144`，用于避免 `run-p` 并行执行类型检查和 Vite 构建时出现临时 Node heap 波动；拆开运行 `npm run type-check` 与 `npm run build-only` 也均已通过。

### 2.2 构建提示

Vite 输出 chunk 体积提醒：

1. `antd` vendor chunk 约 `1,453.43 kB`，gzip 约 `441.19 kB`
2. `echarts` vendor chunk 约 `1,121.30 kB`，gzip 约 `372.09 kB`

主入口 `index` JS 已降至约 `28.25 kB`，统计页页面 chunk 约 `6.11 kB`。Vite 仍提示 vendor chunk 超过 500 kB，这不阻塞当前变更；后续可继续推进 Ant Design Vue 按需注册和 ECharts 模块化导入。

### 2.3 视觉与滚动验证

已使用 Playwright 生成并检查：

1. `frontend/.visual-check/home-1440-final-v2.png`
2. `frontend/.visual-check/home-768-final-v2.png`
3. `frontend/.visual-check/home-375-final-v2.png`
4. `frontend/.visual-check/home-375-y1100-v2.png`
5. `frontend/.visual-check/home-375-y1600-v2.png`
6. `frontend/.visual-check/home-375-y3000-v2.png`

结果：1440 / 768 / 375 三档 `htmlScroll` 与 `bodyScroll` 均等于视口宽度，横向溢出节点为 0；375px 与 768px 的滚动局部图确认工具箱、核心能力区和工作流摘要均可访问。

---

## 3. 已推进的 P0 任务

| Issue | 状态 | 说明 |
| --- | --- | --- |
| FP-P0-03 | 已启动 | design token v1 已落地第一版 |
| FP-P0-04 | 已启动 | 核心组件状态底线已收口到 `common.css` |
| FP-P0-01 | 已启动 | 页面代码审计基线已落文档，截图验收待补 |
| FP-P0-02 | 已完成第一版 | 首页/创作页/Skill 执行页 IA 已冻结 |
| FP-P0-06 | 已启动 | 创作页移动端辅助信息区已落第一版 |
| FP-P0-05 | 已启动 | 创作页输入状态已从线性控件流优化为任务区、设置区、提交区三段结构 |
| FP-P0-07 | 已启动 | 创作页输入表单分组、CTA 附近配额反馈和移动端堆叠已落第一版 |
| FP-P0-08 | 已启动 | 创作页最明显 emoji 状态表达已清理 |
| FP-P0-10 | 已启动 | 首页最近创作入口已前置 |
| FP-P0-11 | 已推进 | 首页首屏和核心能力区已新增真实流程证据，减少泛营销式表达 |
| FP-P0-15 | 已启动 | reduced-motion 规则已迁移到统一样式入口 |
| FP-P0-16 | 已推进 | 已落地路由懒加载、Vite 稳定依赖分包和根滚动修复，构建与三档视觉检查已通过 |

---

## 4. 下一步建议

下一步建议优先做：

1. `FP-P0-05`：开始创作页桌面结构重构
2. `FP-P0-07`：优化创作页输入区分组与表单结构
3. `FP-P0-11`：重构首页证据区与任务型内容模块
4. `FP-P0-16`：分析主入口与统计页 chunk 过大原因

当前代码已经具备继续做页面结构重构的基础。

---

## 5. 2026-07-26 UI/UX 审计与第一批修复

新增审计文档：`2026-07-26-frontend-uiux-audit-and-execution-plan.md`。

本批已完成：

1. Vite API proxy 支持通过 `VITE_API_PROXY_TARGET` 覆盖，默认端口保持 8567。
2. 创作、历史、文章详情和会员页补充 `requiresAuth`，消除未登录空白主区。
3. GlobalHeader 不再输出 H1，页面主标题层级恢复唯一。
4. 创作页、Skill 中心和 Skill 执行页移除嵌套 main landmark。
5. 首页选题、创作选题、登录和注册输入补充可访问标签关系。
6. 登录和注册表单增加 vertical label、自动填充、loading、disabled 和内联错误恢复。
7. 研究基线扩展为 17 个 W3C、web.dev、MDN、Vue、Vite、GOV.UK、Material、Apple 和 NN/g 来源。

### 5.1 最终浏览器回归

已完成：

1. `/skill/uiux-audit` 在未登录状态下跳转至 `/user/login?redirect=/skill/uiux-audit`。
2. 无效账号登录后，表单内显示“用户不存在或密码错误”，并使用 `aria-live="polite"` 保留可访问错误反馈。
3. 首页最终 DOM 检查为 1 个 H1、1 个 main、0 个嵌套 main，选题输入具备 label 和 `aria-describedby`。
4. 当前浏览器窄视口下未出现横向溢出，前端保留在 `http://127.0.0.1:5173/` 供检查。

限制：缺少可用的应用层登录账号，因此登录后的创作、历史、详情和会员页面未完成真实数据端到端回归；本轮已验证认证边界，深层页面继续以代码审计和既有截图基线为依据。

## 6. 2026-07-26 认证页组件与视觉收口

本批继续执行 UI/UX 审计计划 Phase B：

1. 新增 `AuthFormShell`，统一登录与注册页的容器、标题、响应式布局和表单宽度。
2. 登录与注册页改为复用 `AuthBrandSection`，删除两份重复的品牌区与页面结构样式。
3. 品牌区移除无证据的“10万+”结果承诺，改为选题、结构确认、正文配图和历史成果等可验证流程价值。
4. 移除持续脉冲渐变背景与 `backdrop-filter`，改用静态深色品牌面和低成本几何强调。
5. 错误提示支持关闭并保持 `aria-live`，提交按钮同时表达 loading 文案、锁定和视觉状态。
6. 登录页保持 2 个持久标签，注册页保持 3 个持久标签；两页均只有一个 H1。

验证结果：

1. `npm run type-check` 通过。
2. 认证页四个相关 Vue 文件的定向 ESLint 通过。
3. `npm run build-only` 通过；现有 Ant Design 与 ECharts vendor chunk 警告保持不变。
4. 浏览器验证 1440 与 375 宽度下登录、注册页均无横向溢出。
5. 页面控制台无 error 或 warning。

## 7. 2026-07-26 Skill 中心与结果动作收口

本批继续执行 UI/UX 审计计划 Phase B：

1. Skill 中心从三张等权同构卡片改为紧凑任务目录，降低无效留白并提升横向比较效率。
2. 配置层为每个公开 Skill 增加“你需要提供”和“你将得到”，卡片直接解释输入与产出。
3. Skill 中心固定使用前端产品文案，不再由后端旧描述覆盖统一口径。
4. 桌面端使用任务契约列，移动端重排为单列信息流，保留完整输入、产出与入口信息。
5. 新增统一结果复制与 Markdown 下载工具，集中处理成功和失败反馈。
6. 选题、审校、社交转写及通用结果均支持复制和 Markdown 下载。
7. 选题结果继续保留“使用此选题”作为主任务动作，执行完成区保留“调整输入”入口。

验证结果：

1. `npm run type-check` 通过。
2. 本批涉及的配置、工具和结果组件定向 ESLint 通过。
3. `npm run build-only` 通过；现有 Ant Design 与 ECharts vendor chunk 警告保持不变。
4. 浏览器验证 Skill 中心在 1440 与 375 宽度下无横向溢出。
5. Skill 中心保持单一 H1、单一 main、无嵌套 main。
6. 三个公开 Skill 均正确展示输入与产出契约，浏览器控制台无 error 或 warning。

## 8. 2026-07-26 统计页信息与状态收口

本批进入 UI/UX 审计计划 Phase C：

1. 新增可复用 `ChartPanel`，统一图表标题、时间范围、文本摘要、加载、空数据和错误恢复状态。
2. 统计页将关键指标前置，并为每个指标补充口径说明与最后更新时间。
3. 创作趋势和用户构成图增加可见文本结论、`aria-label` 与图表下方数据明细，图表不可见时仍能理解核心数据。
4. 首次加载失败改为页面内错误状态和重试动作；刷新失败时保留上一次成功数据并显示持久提醒。
5. 移动端将指标、图表和性能数据重排为单列，不再直接压缩桌面双列布局。
6. 删除“总用户数 × 5”推算总配额及剩余配额的错误逻辑；后端仅提供累计使用量，页面改为如实展示累计配额消耗。
7. 使用 `nextTick` 替代固定 100ms 延迟渲染图表，减少时序不稳定。

验证结果：

1. `npm run type-check` 通过。
2. `ChartPanel` 与统计页定向 ESLint 通过。
3. `npm run build-only` 通过；统计页业务 chunk 约 8.58 kB（gzip 约 3.66 kB）。
4. 未登录访问 `/admin/statistics` 会跳转 `/user/login?redirect=/admin/statistics`，后台认证边界有效。
5. 当前没有可用管理员测试账号，因此登录后 1440 / 375 真实数据页面未执行端到端截图；响应式结构以代码审查、类型检查和生产构建为验收基础。

## 9. 2026-07-26 历史文章列表收口

本批继续执行 UI/UX 审计计划 Phase C：

1. 历史页重构为桌面高密度表格与移动端文章列表两套真实布局，手机端不再依赖横向滚动表格。
2. 筛选区增加持久字段标签，375 宽度下默认折叠，并通过 `aria-expanded` 表达展开状态。
3. 状态筛选改为使用后端已支持的 `status` 参数；标题与日期仍为当前页过滤，并明确显示“本页匹配 / 全部记录”，避免错误暗示全量搜索。
4. 增加首次加载错误、保留旧数据的刷新错误、加载骨架、无历史数据和无筛选结果状态。
5. 完成状态才能导出；失败状态提供重试；删除动作补充不可恢复说明并校验后端业务响应。
6. 删除当前页最后一篇文章且不在第一页时，会自动回到上一页再刷新。
7. 复用 `StatusBadge` 与 `exportAsMarkdown`，移除页面内重复状态样式和导出实现。

验证结果：

1. `npm run type-check` 通过。
2. 历史页定向 ESLint 通过。
3. `npm run build-only` 与 `git diff --check` 通过；历史页业务 chunk 约 10.01 kB（gzip 约 4.36 kB）。
4. 375 宽度下未登录访问 `/article/list` 会跳转 `/user/login?redirect=/article/list`。
5. 认证跳转页保持单一 H1、单一 main、无横向溢出，控制台无 error 或 warning。
6. 当前没有可用应用测试账号，登录后真实文章列表的桌面与移动端截图回归仍受限，已通过代码审查、类型检查和生产构建覆盖。

## 10. 2026-07-26 文章阅读与结果消费体验收口

本批继续执行 UI/UX 审计计划 Phase C：

1. 新增共享 `ArticleReadingView`，由文章详情页和创作完成页共同承担标题、元信息、大纲、正文与配图渲染，消除两套阅读结构逐步漂移的问题。
2. 阅读正文稳定在 760px 最大宽度，并统一标题层级、正文行高、段落节奏、引用、代码、表格和图片样式；移动端进一步收紧字号与间距。
3. 大纲改为原生可折叠内容，正文为空时提供明确空状态，配图使用懒加载并保留替代文本。
4. 详情页将“返回、转社交媒体、重新创作、导出 Markdown”集中为任务动作区；无正文时禁用导出，导出成功与失败继续复用统一反馈工具。
5. 首次加载增加骨架屏；请求失败和记录不存在分别使用页面内错误状态与 404 状态，并提供重试或返回历史入口。
6. 执行记录降级为正文后的次级可折叠信息，独立覆盖加载、错误、空数据和重试状态，不再与文章主阅读路径竞争。
7. 文章和执行记录请求均校验业务响应码；文章读取失败不再叠加瞬时 toast，避免页面错误状态与全局提示重复。
8. 创作完成页保留“已完成”和审校入口，同时直接复用详情页的阅读核心，确保用户从创作完成到历史回看时层级和排版连续。

验证结果：

1. `npm run type-check` 通过。
2. `ArticleReadingView`、文章详情页与创作完成页定向 ESLint 通过。
3. `npm run build-only` 通过，3943 个模块完成转换；阅读组件业务 chunk 约 3.47 kB（gzip 约 1.77 kB），详情页业务 chunk 约 6.21 kB（gzip 约 2.91 kB）。
4. `git diff --check` 通过。
5. 375 宽度下访问受保护详情路由会进入 `/user/login?redirect=/article/test-task-id`，保持单一 H1、单一 main 且无横向溢出。
6. 1440 宽度下未登录访问详情路由会稳定显示登录拦截状态，保持单一 main 且无横向溢出；控制台仅有预期的未登录用户请求告警。
7. 当前没有可用应用测试账号，因此真实文章数据态的桌面与移动端截图回归仍受限；共享阅读组件、状态分支和响应式布局已通过代码审查、类型检查、定向 Lint 与生产构建覆盖。

## 11. 2026-07-26 会员页与 VIP 状态体验收口

本批继续执行 UI/UX 审计计划 Phase C，并使用新增的本地 VIP 测试账号完成登录后真实状态回归：

1. 会员页从独立营销式落地页重构为产品内“会员与权限”状态页，移除大面积渐变 Hero、悬浮价格卡和重复权益卡片。
2. VIP、管理员和基础账号分别显示真实标题、状态说明、账号事实与下一步动作；VIP 页面不再继续提示“升级永久会员”。
3. 权益范围严格收口为后端已实现的四项能力：无限文章创作配额、Nano Banana AI 配图、SVG 概念示意图和 AI 大纲编辑。
4. 删除无法由当前实现证明的“限时优惠”“原价 $299”“7 天无理由退款”和“优先生成队列”等承诺。
5. 支付成功回跳会重新获取账号状态，只有确认 VIP 或管理员权限后才提示生效；状态尚未同步或请求失败时提供持久说明与重试入口。
6. 支付取消和创建支付失败改为页面内反馈，修正失败提示错字；支付说明仅保留 Stripe 托管和一次购买等可验证信息。
7. 权益列表采用高密度信息行，FAQ 改为原生折叠结构；移动端将状态、事实、动作和能力列表重排为单列。
8. 使用本地 `vip_test` 账号完成真实 VIP 状态验证，页面正确显示开通日期、永久有效、四项已解锁能力及“开始创作”动作。

验证结果：

1. `npm run build` 通过，类型检查与 Vite 生产构建均成功；会员页业务 chunk 约 7.89 kB（gzip 约 3.46 kB）。
2. 全量前端 ESLint 通过。
3. `npm run test:skill` 通过，4 项前端状态与工具测试全部成功。
4. `mvn test` 通过，17 项后端测试中 15 项成功、2 项按环境条件跳过，无失败或错误。
5. 浏览器验证 1440 × 960 与 375 × 812 两个视口，页面均无横向溢出。
6. VIP 状态页保持单一 H1、单一 main；桌面端和移动端均正确显示“永久会员权益”“永久会员已生效”和 4 项“已解锁”状态。

## 12. 2026-07-26 用户管理页与管理员测试闭环

本批继续执行 UI/UX 审计计划 Phase C，并补齐用户管理页原本缺失的服务端契约与本地管理员测试条件：

1. 用户管理页重构为管理员任务面：顶部提供总量与刷新，筛选区支持账号、用户名和角色，主体在桌面端使用高密度表格，在移动端使用真实用户列表，不再压缩或横向滚动桌面表格。
2. 页面补齐首次加载骨架、首次错误与重试、刷新失败但保留旧数据、无用户、无筛选结果和持久成功反馈；筛选、清除、分页和刷新均校验业务响应。
3. 用户详情与编辑集中到抽屉，展示身份、用户 ID、创建时间、角色、用户名和简介；危险操作单独分区，并通过不可恢复说明和二次确认降低误删风险。
4. 当前管理员账号不能删除自己，也不能移除自己的管理员角色；前端禁用并解释原因，后端再次校验，避免绕过页面直接提交。
5. 后端新增管理员专用 `/user/list/page/vo` 和 `/user/update`，限制最大分页量与可用角色，支持账号、用户名和角色条件查询，并只返回不含密码的 `UserVO`。
6. 修正前端将 Java Snowflake ID 当作 JavaScript number 处理造成的精度丢失：登录用户、用户列表与更新请求中的 ID 统一按字符串序列化和传递。
7. 新增仅在 local/test profile 生效的幂等管理员测试账号初始化器；默认账号为 `admin_test`，密码为 `AdminTest@2026`，可通过 `TEST_ADMIN_ACCOUNT_*` 环境变量覆盖或关闭。
8. 新增用户管理集成测试，覆盖管理员登录、用户列表、ID 字符串序列化、密码不泄露、资料更新、自删除拦截和其他用户删除。

验证结果：

1. `npm run type-check` 与全量前端 ESLint 通过。
2. `npm run build` 通过，3943 个模块完成转换；用户管理页业务 chunk 约 13.25 kB（gzip 约 4.84 kB），现有 Ant Design 与 ECharts vendor chunk 警告保持不变。
3. `npm run test:skill` 通过，4 项前端状态与工具测试全部成功。
4. `mvn test` 通过，18 项后端测试中 16 项成功、2 项按环境条件跳过，无失败或错误。
5. 使用真实 `admin_test` 登录验证 5 条用户数据，筛选、清除、资料更新和当前账号保护均按预期工作。
6. 浏览器验证 1440 × 960 与 375 × 812 两个视口：桌面表格和移动列表均无横向溢出，页面保持单一 H1、单一 main。
7. 移动回归发现并修复固定 440px 编辑抽屉在 375px 视口裁切内容的问题；修复后抽屉宽度与视口一致，标题、字段和操作均可完整访问。

## 13. 2026-07-26 Phase D 首批依赖体积治理

本批进入 UI/UX 审计计划 Phase D，优先处理构建报告中最大的 Ant Design Vue 与 ECharts vendor：

1. 移除 `app.use(Antd)` 全量组件注册，改为只注册当前模板实际使用的 26 个 Ant Design Vue 组件插件；不引入新的运行时或构建依赖。
2. 统计页不再通过 `import * as echarts` 引入完整 ECharts，改为组合柱状图、饼图、网格、图例、提示框和 Canvas 渲染器。
3. ECharts 配置类型改为 `ComposeOption` 精确组合，与模块化运行时代码保持一致，避免退回宽泛或无类型配置。
4. 新增 `scripts/check-bundle-size.mjs`，生产构建后自动校验 Ant Design、ECharts 和最大业务 chunk 的原始与 gzip 体积。
5. 标准 `npm run build` 调整为依次执行类型检查、生产构建和体积预算；预算失败会以非零状态阻断构建。
6. 当前预算为：Ant Design 原始 1050 KiB / gzip 320 KiB，ECharts 原始 550 KiB / gzip 190 KiB，单个业务 chunk gzip 120 KiB。

体积结果：

1. ECharts 从 1121.29 kB 降至 518.86 kB，约减少 53.7%；gzip 从 372.08 kB 降至 174.69 kB，约减少 53.1%。
2. Ant Design 从 1449.01 kB 降至 986.07 kB，约减少 31.9%；gzip 从 440.14 kB 降至 300.15 kB，约减少 31.8%。
3. 最大业务 chunk 为入口业务代码，约 29.55 kB（gzip 约 11.87 kB），显著低于 120 KiB 预算。

验证结果：

1. 标准 `npm run build` 通过，并实际执行体积预算检查。
2. 全量前端 ESLint 与 `git diff --check` 通过。
3. `npm run test:skill` 通过，4 项测试全部成功。
4. 浏览器覆盖首页、登录、注册、创作、Skill 中心、历史、会员、统计和用户管理 9 个主要路由，未发现未注册的 `a-*` 组件或运行时 warning/error。
5. 1440 × 960 与 375 × 812 下重点回归首页、创作、统计和用户管理，均保持单一 H1、单一 main 且无横向溢出。
6. 本地统计接口因现有 Redis 认证配置返回错误，页面正确进入持久错误与重试状态；ECharts 模块完成加载且无运行时异常，但真实图表数据态仍需在可用统计接口环境中补充回归。
7. Ant Design 与 ECharts 产物仍超过 Vite 默认 500 kB 提示线，后续 Phase D 应继续推进路由级组件拆分与 vendor 分包，而不是提高警告阈值。

## 14. 2026-07-26 Phase D 路由级组件分包

本批继续执行 Phase D，将首批“按需注册”进一步推进为真正的路由级代码分割：

1. 应用入口只保留外壳、导航和首页立即需要的 Ant Design 组件。
2. 表格、日期范围、抽屉、表单、分页、选择器、确认框和模态框改由对应懒加载路由局部导入。
3. Alert、Radio、Checkbox、Progress、Segmented 和 Tooltip 等任务组件也移入认证、创作、Skill、会员和后台页面，避免进入公共首屏。
4. 移除将 `ant-design-vue` 与全部图标强制合并到单一 `antd` vendor 的 `manualChunks` 配置，让 Rollup 按公共依赖和路由边界生成共享 chunk。
5. 局部组件类型暴露出并修复三处旧类型缺口：用户表格列对齐类型、历史日期范围清空值、Skill 下拉框的 `unknown` 值。
6. 构建预算不再依赖固定的 `antd-*.js` 文件名，改为解析 `index.html`，校验首屏入口 chunk、首屏全部 JavaScript、ECharts 和最大延迟 chunk。
7. 当前预算为：入口 488 KiB / gzip 160 KiB，首屏 JS 合计 720 KiB / gzip 245 KiB，ECharts 550 KiB / gzip 190 KiB，最大延迟 chunk 200 KiB / gzip 70 KiB。
8. 评估过将两个统计图切换为 SVG 渲染器，但产物由 518.86 kB 增至 527.37 kB，因此恢复 Canvas，保留更小实现。

体积结果：

1. 首屏入口 chunk 为 478.57 kB（gzip 149.87 kB），已低于 Vite 500 kB 提示线。
2. 首屏必要 JavaScript 由约 1229.34 kB 降至 699.71 kB，约减少 43.1%；gzip 由约 390.84 kB 降至 231.75 kB，约减少 40.7%。
3. 最大延迟共享 chunk 为 177.05 kB（gzip 53.54 kB），历史页路由 chunk 为 116.51 kB（gzip 36.26 kB），均在新预算内。
4. ECharts 保持 518.86 kB（gzip 174.69 kB）；当前 Vite 大 chunk 提示只对应进入统计路由后才加载的 ECharts，不再对应首屏入口。

验证结果：

1. 标准 `npm run build` 通过，类型检查、生产构建和新版体积预算全部成功。
2. 全量前端 ESLint、`npm run test:skill` 4 项测试和 `git diff --check` 通过。
3. 浏览器覆盖 9 个主要路由，所有局部组件均成功解析，未发现残留 `a-*` 标签或运行时 warning/error。
4. 实际操作验证登录表单、创作页单选与复选、Skill 分段控件、历史状态筛选、用户表格与编辑抽屉均正常。
5. 1440 × 960 与 375 × 812 下页面保持单一 H1、单一 main、无横向溢出；移动用户抽屉仍保持 375px 宽度且左边界为 0。

## 15. 2026-07-26 Phase D 语义与响应式回归收口

本批继续执行 Phase D，将浏览器回归中发现的语义缺口修复并完成四档视口验收：

1. Skill 执行页在输入态补齐 H1，并在执行中、失败和完成状态持续保留 Skill 名称作为页面主标题；嵌入式执行面板继续使用 H2，避免宿主页出现重复 H1。
2. 未知 Skill 的可恢复错误态改为语义化主标题，保留重新加载和返回 Skill 中心路径。
3. 用户管理筛选表单和编辑表单为所有字段补充 `name`，并通过独立表单命名空间消除重复 DOM id 与标签交叉关联。
4. 用户管理分页器的每页数量下拉框补充“每页显示用户数量”可访问名称，保留桌面端分页容量切换能力。
5. 浏览器回归同时检查实际路由、单一 H1、单一 main、嵌套 main、可见表单标签、重复 id、横向溢出和控制台 warning/error。

验证结果：

1. 11 个核心路由在 375、768、1024、1440 四档宽度下完成 44 组回归，全部通过且无横向溢出。
2. 所有回归页面均保持单一 H1、单一 main、无嵌套 main；可见输入控件均有程序化标签，页面无重复 id。
3. 未知 Skill 正确显示“这个技能暂不可用”H1 和恢复动作；Skill 输入态正确显示具体 Skill 名称 H1。
4. 用户管理编辑抽屉打开后，筛选与编辑字段仍各自关联到唯一标签；分页容量控件可按 accessible name 定位。
5. 干净浏览器会话下 44 组回归无 console warning 或 error。
6. 标准 `npm run build` 通过，类型检查、生产构建和体积预算全部成功；首屏 JS 合计 gzip 约 226.31 KiB，最大延迟 chunk gzip 约 52.29 KiB。
7. 全量前端 ESLint、`npm run test:skill` 4 项测试和 `git diff --check` 通过。

## 16. 2026-07-26 Phase D 自动化 UI 门禁与键盘焦点修复

本批将上一批人工浏览器验收固化为可重复的前端检查，并修复自动化发现的焦点可见性缺陷：

1. 新增 `@playwright/test`、`playwright.config.ts` 和 `tests/ui/core-routes.spec.ts`，覆盖 11 个核心路由在 375、768、1024、1440 四档视口下的 H1/main、嵌套 landmark、横向溢出、可见控件标签、重复 id 和运行时错误检查。
2. UI 门禁使用本地 `admin_test` 登录验证受保护路由，并加入登录键盘路径、可见焦点环和 `prefers-reduced-motion` 持续动画检查。
3. 新增串行 `npm run check`，依次执行只读 ESLint、类型检查、生产构建/体积预算、状态测试和 Playwright UI 回归，避免构建与浏览器并行造成内存竞争。
4. 删除全局 `:focus` 的轮廓清除规则，并提高 `:focus-visible` 焦点环优先级，修复 Ant Design `a:focus { outline: 0 }` 覆盖品牌链接键盘焦点的问题。
5. 前端 README 补充 UI 测试前置条件、测试账号和环境变量说明；Playwright 产物目录加入 `.gitignore`。

验证结果：

1. `npm run test:ui` 6/6 通过：4 档视口核心路由、键盘导航、reduced-motion 全部通过。
2. 独立浏览器会话确认品牌链接 `:focus-visible` 匹配，计算样式为绿色实线焦点环，页面无 warning/error。
3. `npm run check` 串行通过，包含全量 ESLint、类型检查、构建体积预算、4 项 Skill 状态测试和 6 项 Playwright 测试。
4. 最终构建预算：首屏 JS gzip 约 226.32 KiB，ECharts gzip 约 170.59 KiB，最大延迟 chunk gzip 约 52.29 KiB。
