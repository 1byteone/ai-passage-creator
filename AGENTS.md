# Project Agent Guide

## 项目定位

这是一个 Spring Boot 3 + Java 21 + MyBatis-Flex + Vue 3 + TypeScript 的 AI 文章创作平台。主要域包括文章创作、Skill 工作流、漫画手帐、英语训练、数据分析和手写排版。

## 二次开发经验

### 先确认，再生成

- 需求涉及原型、页面流程或验收走查时，先读取 PRD、已有路由和接口契约。
- 首轮只输出页面清单、状态清单、操作和跳转关系，不生成 HTML、不截图、不修改业务代码。
- 用户确认范围后再实施，并将未明确的内容标记为“待确认”，禁止自行补全产品能力。

### 外部项目集成

- 优先吸收产品理念和公开 API，不整体复制外部仓库源码、页面或资源。
- 先确认许可证、资源版权和分发义务，再选择 npm 依赖、适配层或独立重实现。
- 保持当前项目的路由、Session、错误处理、测试框架和视觉系统，不建立平行基础设施。
- 本项目的 Pretext 集成采用 `@chenglou/pretext` + `frontend/src/services/textLayout.ts` 适配层，浏览器能力不足时使用确定性降级。

### PM 原型交付标准

- 低保真原型只表达信息层级、核心文案、按钮、页面状态和流程关系。
- 使用黑白灰、基础边框和图片占位，不制作正式视觉 UI。
- 单页单文件 HTML，打开即可查看，不依赖外部资源或构建服务。
- 按真实使用顺序平铺正常流程、首次使用、权限、加载、无数据、失败和恢复状态。
- 每一屏必须标注页面名、流程位置、可执行操作、操作结果和待确认项。

### 页面全流程走查标准

- 按用户真实路径分组，每组覆盖正常路径、关键分支和状态变化。
- 每个页面状态必须有复现步骤、反馈输入框和“可以 / 待改 / 阻塞”标记。
- 反馈自动保存到浏览器本地存储；支持 Markdown 和 JSON 导出。
- 新一轮走查保留上一轮反馈原话，并标记发生改动的页面，支持逐条回归。
- 反馈导出前检查是否含密码、Token、API Key、个人信息或其他敏感数据。

## 工程质量门

- 前端至少执行 `npm run type-check`、`npm run build-only` 和相关 Playwright 测试。
- Skill 或排版逻辑变更必须有 Node test；跨前后端契约变更必须有接口或集成测试。
- 真实联调需核验前端、后端、数据库、Redis、Session 和外部对象存储，不以测试桩结果代替。
- 测试数据必须可重复、可识别、不可依赖生产用户；本地管理员测试账号只用于本地验收。
- 不回滚用户已有改动；提交前检查 `git diff`，确认无密钥、Cookie、临时日志和生成物进入交付。

## 可复用技能

- `skills/vibecoding-pm-workflow/SKILL.md`：外部项目二次开发时的原型与流程验收方法，不是当前产品业务模块；需要进行页面原型或全流程走查时按该规范执行。
